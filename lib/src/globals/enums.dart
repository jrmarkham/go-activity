
// UI
// enum NavScreens {home, add, edit}
enum NavDisplay {wait, list, detail, add, edit}

// FORMS
enum FieldState { empty, invalid, valid }
enum FieldType { title, date, location, description, imageUrl }


